<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medpal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo1_round"
        android:label="@string/applicationName"
        android:roundIcon="@mipmap/logo1_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn"
        android:parentActivityName=".MainActivity">
        </activity>
        <activity android:name=".Register"
        android:parentActivityName=".MainActivity">
        </activity>
        <activity android:name=".MedicalRecord">
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".ImportantContacts">
        </activity>
        <activity android:name=".ImportantContactsEdit" android:parentActivityName="com.example.medpal.ImportantContacts">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.CALL_PHONE" />

</manifest>